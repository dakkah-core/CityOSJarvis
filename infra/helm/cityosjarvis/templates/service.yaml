apiVersion: v1
kind: Service
metadata:
  name: {{ include "cityosjarvis.fullname" . }}
  labels:
    {{- include "cityosjarvis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cityosjarvis.selectorLabels" . | nindent 4 }}
